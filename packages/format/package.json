{
  "name": "@bzm/vitals-format",
  "version": "0.1.0",
  "private": true,
  "description": "Format v1 — the spine. The collector's only output and the dashboard's only input. A format change is one PR touching writer and reader together.",
  "type": "module",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "scripts": {
    "build": "tsc --build",
    "typecheck": "tsc --build"
  }
}
